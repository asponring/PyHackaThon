#declaring lists
array = []

#length
length = len(array)

#adding to an array
array.append("a pen")
array.append("another pen")

#index retrieval
firstItem = array[0]
secondItem = array[1]
print (firstItem, secondItem)

